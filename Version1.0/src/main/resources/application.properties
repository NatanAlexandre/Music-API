#SQL

# Separar cores no console
spring.output.ansi.enabled=ALWAYS

# Database config
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver
spring.datasource.url=jdbc:mysql://localhost:3306/music_api?createDatabaseIfNotExist=true

# Database Login
spring.datasource.username=root
spring.datasource.password=Serc@1010

# Cria tabelas caso não existam e atualiza tabelas automaticamente
spring.jpa.hibernate.ddl-auto=update

# Mostra SQL no terminal
spring.jpa.show-sql=true

server.error.include-exception = false
